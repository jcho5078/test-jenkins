spring:
  application:
    name: testRds

  thymeleaf:
    cache: false
    check-template: true
    prefix: classpath:templates/
    suffix: .html

  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
      path: /h2-console

  datasource:
    url: jdbc:mariadb://database-1.cmdh7ovqnvel.ap-northeast-2.rds.amazonaws.com:3306/test
    #url: jdbc:mariadb:// /test
    driver-class-name:  org.mariadb.jdbc.Driver
    username: admin
    password: 98989898

  jpa:
    hibernate:
      ddl-auto: create-drop
      #naming:
        #physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    generate-ddl: true


logging:
  level:
    com.example.catalogservice: DEBUG